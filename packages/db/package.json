{
  "name": "@azalas/db",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "migrate:dev": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy",
    "seed": "tsx seed.ts",
    "import": "tsx import.ts",
    "import:critical": "tsx import-legacy-critical.ts",
    "verify:critical": "tsx verify-legacy-critical.ts",
    "validate": "prisma validate"
  },
  "dependencies": {
    "@prisma/client": "^5.18.0",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "prisma": "^5.18.0",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0"
  }
}
